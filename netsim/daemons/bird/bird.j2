{% set module = module|default([]) %}
log "/var/log/bird" all;

{% include 'protocols.j2' %}

{% set lla_bgp = bgp.neighbors|default([])|selectattr('local_if', 'defined')|list if bgp is defined else [] %}
{% if lla_bgp or ('ipv6' in af and role != 'host') %}
{%   include 'radv.j2' %}
{% endif %}

{% for k,v in _daemon_config.items() if k != device|default(netlab_device_type) and not v.endswith('.sh') %}
include "{{ v }}";
{% endfor %}
